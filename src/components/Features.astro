---
import Card from "./Cards.astro";

const featuresData = [
    {title: 'Card Title 1', body: 'Card Body Text Goes Here.'}, 
    {title: 'Card Title 2', body: 'Card Body Text Goes Here'}, 
    {title: 'Card Title 3', body: 'Card Body Text Goes Here'}, 
    {title: 'Card Title 4', body: 'Card Body Text Goes Here '}
]
---

<section class="features">
    <div class="container">
      {featuresData.map(feature => (
        <Card dark={true}>
          <h3>{feature.title}</h3>
          <p>{feature.body}</p>
        </Card>
      ))}
    </div>
  </section>

  <style>
    .features .container {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  grid-gap: 20px;
  padding: 0 20px;
  margin: 30px auto;
}

@media (max-width: 500px) {
  .features .container {
    grid-template-columns: 1fr;
  }
}
  </style>